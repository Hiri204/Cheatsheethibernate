<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <context:component-scan base-package="cheatsheethibernate" />

    <mvc:annotation-driven />
    <tx:annotation-driven transaction-manager="transactionManager" />

    <!-- ✅ Static Resources - ပုံတွေအတွက် -->
    <mvc:resources mapping="/uploads/**" location="/uploads/" />
    <mvc:resources mapping="/images/**" location="/images/" />
    <mvc:resources mapping="/css/**" location="/css/" />
    <mvc:resources mapping="/js/**" location="/js/" />

    <!-- ✅ Default Servlet Handler - မရှိတဲ့ Resource တွေအတွက် -->
    <mvc:default-servlet-handler />

    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="5242880" />
        <property name="defaultEncoding" value="UTF-8" />
    </bean>

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <bean class="org.springframework.context.support.ResourceBundleMessageSource" id="messageSource">
        <property name="basename" value="messages" />
    </bean>

    <!-- ✅ Interceptor with Public URLs excluded -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/cheatsheets/**"/>
            
            <!-- ✅ Public URLs တွေကို မစစ်ပါနဲ့ -->
            <mvc:exclude-mapping path="/cheatsheets/category/**"/>
            <mvc:exclude-mapping path="/cheatsheets/view/**"/>
            <mvc:exclude-mapping path="/cheatsheets/tag/**"/>
            <mvc:exclude-mapping path="/cheatsheets/search"/>
            <mvc:exclude-mapping path="/cheatsheets/comments/**"/>
            
            <!-- ✅ Images, Uploads တွေကိုလည်း မစစ်ပါနဲ့ -->
            <mvc:exclude-mapping path="/images/**"/>
            <mvc:exclude-mapping path="/uploads/**"/>
            <mvc:exclude-mapping path="/css/**"/>
            <mvc:exclude-mapping path="/js/**"/>
            
            <bean class="cheatsheethibernate.interceptor.AuthInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>

</beans>